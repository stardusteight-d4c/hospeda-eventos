export const headerStyles = {
  wrapper: `flex flex-col md:flex-row items-center justify-between w-full mt-6`,
}

export const eventTableStyles = {
  wrapper: `overflow-x-scroll lg:overflow-visible border border-layout-body rounded-xl mt-6 mb-10`,
  container: `flex flex-col min-w-[800px] w-full`,
}
